# rv-tui-frontend

## Setup

## Prerequisites
- install `node`,`npm`

```bash
$ npm install
  npm run build
  npm link
```
After this the app can be lauched with
```bash
$ npm run dev
```
```bash
$ rv
```

- Linting with IDE extension of NPM scripts
